# O projeto Lis opera 100% com a Biblioteca Padrão do Python (Python Standard Library).
# Não há necessidade de instalar pacotes externos via pip!
# O modelo de linguagem (Gemma 4) é acessado via HTTP local (llama-server no Docker).
